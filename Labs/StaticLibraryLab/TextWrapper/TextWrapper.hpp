#include <string>
#include "pch.h"


#ifndef TEXT_WRAPPER
#define TEXT_WRAPPER

bool is_number(const std::string& s);
void wrap(const std::string& fileN, const int& wrapLenght);

#endif // !TEXT_WRAPPER
